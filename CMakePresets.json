{
  "version": 4,
  "configurePresets": [
    {
      "name": "default",
      "displayName": "Default",
      "description": "Default presets with exporting compile commands to improve IDE support with LSPs such as clangd",
      "binaryDir": "${sourceDir}/build",
      "cacheVariables": {
        "CMAKE_EXPORT_COMPILE_COMMANDS": "ON"
      }
    }
  ]
}
